let name = "Ali";// string datatype
let age = 20; // number datatype
let isupset= false; // boolean datatype
let amount = null; // object datatype
let accountno; // undefined datatype

//how to know the type of data?

console.log(typeof(name)); // string
console.log(typeof(age)); // number
console.log(typeof(isupset)); // boolean
console.log(typeof(amount)); // object (null is a special case)
console.log(typeof(accountno)); // undefined